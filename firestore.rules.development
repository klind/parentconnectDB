rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This allows all access - ONLY use for development/testing!
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 